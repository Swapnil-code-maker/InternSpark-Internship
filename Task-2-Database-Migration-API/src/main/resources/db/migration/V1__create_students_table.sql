CREATE TABLE students (
                          id BIGINT PRIMARY KEY,
                          name VARCHAR(100),
                          email VARCHAR(100),
                          course VARCHAR(100)
);